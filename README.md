# Taller de python (DataFrame)

```sh
--Pasos a seguir para tener el taller hecho...

git clone
cd TallerDataFrae
python3 -m venv env(Si no da ese comando)
python -m venv env(utilicen este)
source env/bin/activate
pip3 install -r requirements.txt
python3 main.py
```
